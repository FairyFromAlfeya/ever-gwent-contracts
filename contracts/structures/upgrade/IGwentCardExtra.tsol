pragma ever-solidity ^0.62.0;

import "../IGwentCardParams.tsol";

/// @dev Card's contract assets that must not affect its address during deploy
interface IGwentCardExtra {
    struct GwentCardExtra {
        address recipient;
        string json;
        TvmCell indexCode;
        IGwentCardParams.GwentCardParams params;
    }
}
