pragma ever-solidity ^0.62.0;

/// @dev Values are power of 2 to encode all effects in uint8
/// @dev Can be decoded from uint 8 by using ^ operator
library GwentEffectType {
    uint8 constant GWENT_EFFECT_TYPE_SCORCH = 1;
    uint8 constant GWENT_EFFECT_TYPE_DECOY = 2;
    uint8 constant GWENT_EFFECT_TYPE_COMMANDERS_HORN = 4;
    uint8 constant GWENT_EFFECT_TYPE_SUMMON_AVENGER = 8;
    uint8 constant GWENT_EFFECT_TYPE_MARDROEME = 16;
    uint8 constant GWENT_EFFECT_TYPE_BERSERKER = 32;
}
